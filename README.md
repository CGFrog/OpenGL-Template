# OpenGL-Template

To run this repo do

```
git clone <repo>
cd <repo>

git submodule update --init --recursive

mkdir build
cd build
cmake ..
cmake --build .
```

Then to run do:

```
.\Debug\OpenGLTemplate.exe
```